import {
	simulate,
	netWorthChartData,
	constructCashTableData,
	calculateNetWorth,
	computeLoan
} from './simulation';

for (const [parameter, defaultValue] of parameterDefaults.entries(object1)) {
	console.log(`${parameter}: ${defaultValue}`);
}
